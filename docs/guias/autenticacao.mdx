---
id: autenticacao
title: Autenticação
sidebar_label: Autenticação
sidebar_position: 2
description: Como obter e usar o Bearer token para autenticar requisições à API Notax.
---

# Autenticação

Todas as APIs da Notax usam autenticação via **Bearer token (JWT)**. O fluxo tem dois passos:
trocar suas credenciais por um token e enviar esse token em cada requisição.

## Obtendo o token

Faça um `POST` para [`/api/v1/client/auth`](/notax-pay/autenticar-cliente) enviando suas
credenciais no corpo da requisição:

| Campo          | Tipo            | Obrigatório | Descrição                        |
| -------------- | --------------- | ----------- | -------------------------------- |
| `clientId`     | `string` (UUID) | Sim         | Identificador do cliente.        |
| `clientSecret` | `string`        | Sim         | Segredo associado ao `clientId`. |

```bash
curl -X POST https://api.notax.com.br/api/v1/client/auth \
  -H "Content-Type: application/json" \
  -d '{ "clientId": "<uuid>", "clientSecret": "<secret>" }'
```

## Usando o token

Inclua o token retornado no header `Authorization` de **todas** as requisições subsequentes:

```http
Authorization: Bearer {seu-token}
```

## Boas práticas

- **Nunca** exponha o `clientSecret` no front-end ou em repositórios públicos — trate-o como senha.
- Solicite o token no back-end e reutilize-o enquanto for válido, em vez de autenticar a cada chamada.
- Requisições sem token válido retornam `401 Unauthorized`.

:::tip
Erros de autenticação seguem o padrão [RFC 7807](https://datatracker.ietf.org/doc/html/rfc7807),
com um corpo JSON descrevendo o problema. Veja [Ambientes](./ambientes.mdx) para as URLs base.
:::
